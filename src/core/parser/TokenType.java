package core.parser;

public enum TokenType{
    NUMBER,
    IDENTIFIER,
    OPERATOR,
    PLUS, MINUS,
    STAR, SLASH,
    POW,
    LPAREN, RPAREN,
    COMMA, POINT,
    ASSIGN,
    EOF,
    ERROR
}
